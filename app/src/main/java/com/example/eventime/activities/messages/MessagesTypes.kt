package com.example.eventime.activities.messages

enum class Messages {

}

enum class ValidationError {
    SET_AL_LEAST_ONE_HOUR,
    HOUR_ALREADY_EXISTS,
    DATE_ALREADY_EXISTS,
    MISSING_DATA
}